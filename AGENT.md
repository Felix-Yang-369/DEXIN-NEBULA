# 德馨星云开发协作指南

本文件约束参与德馨星云开发的 AI Agent 和开发人员。任何任务开始前必须先阅读本文件与 [README.md](./README.md)。

## 1. 项目定义

- 产品：德馨星云（DEXIN Nebula）
- 定位：德馨淼盛企业数字化运营平台
- 当前版本：`V0.9-alpha`
- 当前阶段：内部验收、部署与真实业务闭环验证
- 主要用户：内部员工、部门负责人、人事、财务、董事长、管理员
- 当前边界：单一组织内部系统，不开放外部注册，不提前建设完整多租户 SaaS

德馨星云不是单独的 OA。CRM、销售、订单、PIM、SRM、采购、WMS、FMS、HRM、OA、BI 和德小馨 AI 共享组织、账号、权限、文件、通知与审计基础能力。

## 2. 当前事实

以下能力已进入代码或数据库，不得再描述为纯规划：

- Supabase Auth、员工账号绑定、角色权限和 PostgreSQL RLS
- OA 审批、请假、报销、用印、公告、通知、周报、制度、文件
- CRM 客户、联系人、跟进、分级、Logo 和多法律实体
- 产品主档、图片、授权价格、库存状态和 Excel 导出
- 报价、销售机会、销售订单、履约、配送和销售应收
- 供应商、采购申请、采购订单、到货、入库和采购应付
- 仓库、库存、批次效期、出入库、调拨、盘点和配送
- 应收应付、账龄、对账单、银行流水、核销、凭证和发票
- HRM 组织、员工、合同、假期、考勤、入离职、职级岗位和绩效
- BI 权限内真实聚合和数据覆盖提示
- 德小馨 DeepSeek 对话、权限内检索、来源引用和审计

以下能力当前只是入口或部分实现，不得写成已完成：

- 新媒体账号、内容、发布计划、平台数据和舆情监控
- 企业宣传完整资料库与发布审核
- 企业活动立项、执行和复盘
- 完整薪资、招聘、培训体系
- 销售退货、采购退货和复杂订单变更
- 银行自动对账、正式总账、税务与金蝶双向集成
- AI 直接执行申请、审批、付款、删除或数据修改

## 3. 当前优先级

按以下顺序推进，除非用户明确调整：

1. 阿里云部署、内测入口、备份和回滚
2. 老板、财务、客服、仓储四类账号权限验收
3. 销售订单 → 出库配送 → 应收 → 收款核销真实验收
4. 采购订单 → 到货入库 → 应付 → 付款核销真实验收
5. 数据口径、重复提交、异常状态与移动端修复
6. V0.10 运营管理真实数据模型与流程
7. 域名备案后配置 HTTPS 和企业微信扫码登录

不要因为页面入口已经存在，就跳过数据模型、权限和流程闭环。

## 4. 导航与模块架构

侧边栏当前稳定分区为：

```text
经营总览
业务管理：CRM、销售、订单、供应链、产品
运营管理：新媒体、企业宣传、企业活动
财务管理：应收、应付、利润、报表、流水、发票
AI 助手：德小馨、BI
组织协同：HRM、OA、系统管理
```

新增页面优先归入上述分区。不要恢复为十多个没有层次的一级菜单，也不要为了调整导航而移动已有路由或重写业务页面。

## 5. 开始任务前

1. 阅读 `README.md`，确认当前版本、部署状态和未完成边界。
2. 查看相关路由、feature、SQL 迁移和测试。
3. 检查工作区现有修改，用户改动视为用户资产。
4. 检查开发服务器是否已运行，避免重复启动和端口漂移。
5. 涉及 Supabase 时确认目标项目与环境，涉及阿里云时先确认当前在线进程。
6. 涉及身份、权限、金额、库存、员工隐私或生产数据时，先明确风险与回滚方式。

如果用户的最新要求改变产品范围，代码完成后同步更新 README 或相关 `docs/`。

## 6. 实际技术基线

当前已使用：

- Next.js 16 App Router
- React 19、TypeScript
- Tailwind CSS 4、shadcn/ui、Base UI、Lucide、Recharts
- Supabase Auth、PostgreSQL、Storage、Supabase JS/SSR
- SQL 迁移：`supabase/migrations`
- Zod、Day.js、Axios、ExcelJS
- npm 与 `package-lock.json`
- 阿里云 ECS、Node.js 22、PM2、Nginx

当前没有使用 Prisma、React Hook Form、Vitest、Playwright。不要把未安装技术写成现状，也不要为了符合旧文档而无目的引入依赖。

新增依赖前必须：

1. 确认现有依赖无法解决问题。
2. 说明具体收益和运行环境影响。
3. 使用 npm，不混用 pnpm 或 yarn。
4. 提交 `package-lock.json` 的对应变化。

## 7. 代码结构

```text
src/app/                 路由、页面、Route Handlers
src/components/ui/       基础 UI
src/components/business/ 跨模块业务组件
src/config/              导航和稳定配置
src/features/            业务模块与服务端动作
src/lib/                 基础设施与通用工具
src/types/               共享类型
supabase/migrations/     PostgreSQL 迁移与事务函数
tests/                   业务规则自动化测试
docs/                    详细设计、流程和接入说明
```

复杂审批、权限、库存和金额逻辑不得只写在页面组件中。跨表写入优先由 PostgreSQL 事务函数或明确的服务层完成。

## 8. 产品开发原则

### 完整闭环优先

每个模块必须形成可验证流程。例如销售不是只有订单表格：

```text
客户与法律实体 → 订单 → 确认 → 履约 → 库存 → 配送 → 应收 → 核销
```

只有 UI、没有数据与权限的功能必须标注“规划入口”或“原型”。

### 真实数据优先

- 无数据源时显示空状态或数据不足说明。
- 禁止用随机数字伪装经营数据。
- 演示数据必须标明环境和用途。
- 云端验收数据应可回滚，不污染正式业务记录。
- BI 指标必须注明时间范围、权限范围和口径。

### 复用共享能力

- 申请复用统一审批引擎。
- 文件复用统一存储与权限校验。
- 通知复用统一通知模型。
- 审计复用统一日志能力。
- 组织、员工、客户、产品、供应商和法律实体保持单一主数据来源。

## 9. 数据库规则

- 所有结构变更新增 SQL 迁移，不修改已执行迁移。
- 核心表保留稳定主键和 `organization_id` 边界。
- 金额使用 PostgreSQL `numeric/decimal`，JS 展示前显式转换。
- 状态使用明确状态机，不用互相冲突的布尔字段。
- 关键记录保留创建人、更新人、时间和审计信息。
- 员工、审批、财务和业务单据优先停用、作废或归档，不直接硬删除。
- 多表关键写入使用事务，避免库存、单据和财务余额不同步。
- 新增外键后检查查询索引。
- 迁移前确认数据库项目、备份、RLS 和回滚路径。

禁止在未知环境中直接执行生产数据清理、批量更新或破坏性迁移。

## 10. 认证、权限与敏感数据

权限必须同时落实在：

1. 页面访问
2. 服务端操作
3. Supabase RLS 或数据库事务函数
4. 文件下载与签名 URL
5. 字段级展示

客户端隐藏按钮不等于权限控制。

敏感数据包括但不限于：身份证、银行卡、工资、合同、私人联系方式、客户价格、供应商结算、库存成本、利润、登录凭证和 API Key。

- 不得写入代码、日志、截图、测试快照或提交记录。
- `SUPABASE_SERVICE_ROLE_KEY`、`WECOM_APP_SECRET`、`DEEPSEEK_API_KEY` 只能在可信服务端使用。
- 服务端操作从会话读取员工身份，不接受客户端自报 `user_id`。
- 高风险操作需要明确确认、权限复核和审计。

## 11. 财务与库存规则

- 销售确认不等于收入确认；当前应收在实际履约后形成。
- 采购订单不等于应付确认；当前应付在实际到货后形成。
- 核销不得让未核销余额小于零。
- 已核销单据不得随意修改类型和原始金额。
- 库存出库必须校验可用数量，并按批次效期规则处理。
- 调拨、盘点、履约、应收应付和凭证联动必须保证事务一致性。
- “预计毛利”与正式会计利润必须清晰区分。

## 12. 德小馨 AI 规则

- AI 继承当前账号权限，不是超级管理员。
- 回答应标明来源；经营数据应标明时间与口径。
- 不向未经批准的外部服务发送员工隐私或敏感经营数据。
- 提交申请、审批、付款、删除和修改数据必须经过用户明确确认，并由服务端重新校验权限。
- AI 服务异常时提供清晰降级，不影响核心业务系统。
- 对话、检索、Token 用量和高风险工具调用应可审计。

## 13. UI 与交互

- 保持德馨星云深蓝、青色、白色的企业科技风，不随意改成官网营销风。
- 高信息密度页面仍需清晰层级、搜索、筛选、空状态和错误状态。
- 桌面优先，但手机必须能完成登录、审批、查询和核心录入。
- 状态不能只依赖颜色表达。
- 表单具备标签、必填、校验、提交中、成功与失败反馈。
- 危险操作需要确认。
- 自定义交互保持键盘与基础无障碍能力。
- 图片使用合理尺寸；不把大图直接塞入列表。

## 14. 服务端与 API

- 输入使用 Zod 或等价的严格服务端校验。
- 外部服务设置超时并限制重试。
- 不把数据库原始错误直接展示给用户。
- 写操作考虑重复提交和幂等性。
- 列表查询设置边界，避免无上限全表读取。
- 上传校验 MIME、扩展名、大小和访问权限。
- 错误日志保留追踪标识，但不得包含凭证和敏感正文。

## 15. 测试与验证

每次代码改动按风险执行：

```bash
npm run check
npm run test:workflow
npm run build
```

当前基线为 44 项自动化测试。新增状态机、金额、日期、权限或库存逻辑时应补充回归测试。

交付前还应验证：

- 受影响页面可访问
- 无权限账号被正确拦截
- 加载、空状态和错误状态存在
- 浏览器控制台与服务端无新增错误
- 手机端核心操作可用
- 涉及数据库时迁移、RLS 和回滚路径明确

不能完成某项验证时必须说明原因，不能用“页面能打开”替代业务验收。

## 16. 部署规则

当前阿里云目录约定：

```text
/srv/dexin/DEXIN-NEBULA             当前在线目录
/srv/dexin/releases/nebula-*        新版本发布目录
PM2: dexin-nebula
应用端口: 3103
Nginx: 80/443 反向代理
```

部署必须使用发布目录：

1. 上传源代码，不上传本地 `.env.local`、缓存和 `node_modules`。
2. 从当前服务器安全复制生产环境变量，权限设为 `600`。
3. 在独立发布目录安装和构建。
4. 构建成功后先从服务器本机验证。
5. 再原子切换目录并重启 PM2。
6. 验证登录和关键路由。
7. 失败时恢复上一目录和 PM2 进程。

不得在新构建成功前覆盖当前在线目录。不得在输出中打印服务器环境变量。

当前 2 GB ECS 在新版本远程构建时出现内存终止。后续优先使用本地/CI 构建产物或降低远程构建并行度，不要反复在生产机上高负载构建。

备案完成前只使用公网 IP 做受控内测。企业微信扫码登录需要正式域名和 HTTPS 回调后再验收。

## 17. Git 与文件安全

- 用户已有改动不得覆盖。
- 不提交 `node_modules`、`.next*`、`.env*`、数据库备份、真实名单或临时文件。
- 不使用 `git reset --hard`、危险清理或大范围删除。
- 大改动拆分清晰提交，但只有用户授权时才推送或发布。
- 真实员工、客户、供应商、价格、库存和财务数据不得用于公开演示。

## 18. 文档同步

以下变化必须同步 README 或 `docs/`：

- 版本、模块或导航架构
- 路由与用户流程
- 角色与数据范围
- 数据模型与迁移
- 环境变量和外部集成
- 部署、备份与恢复方式
- 已完成与规划边界

README 记录当前事实和运行说明；详细设计放入 `docs/`；后续建议建立 `CHANGELOG.md` 记录每次发布。

## 19. 完成标准

功能只有同时满足以下条件才可描述为完成：

- 用户流程闭环
- 服务端与数据库权限生效
- 加载、空、错误、无权限状态完整
- 相关测试和生产构建通过
- 数据库变化有迁移和回滚说明
- 关键动作有审计
- 桌面和手机核心操作可用
- 文档与实际实现一致

向用户汇报时先给结果，再说明影响模块、验证结果、未完成事项、风险和最合适的下一步。
